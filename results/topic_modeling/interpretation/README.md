# Topic model interpretation

This directory contains the manual interpretation of topic-model outputs. The XLSX file is convenient for reading and the CSV version is intended for machine-readable use.

The interpretation should be kept separate from the model files and raw MALLET outputs.
